version: "3"
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: 'app'
        restart: 'always'
        working_dir: '/app'
        volumes:
            - '.:/app'
        ports:
            - '${FORWARD_APP_PORT:-3000}:3000'
